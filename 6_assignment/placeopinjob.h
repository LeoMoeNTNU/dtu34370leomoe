#include "types.h"
//This one is going to write to one higher because it exists that this exists.
//This one doesn actually place in processor, not in job hehe. 
void placeopinjob(op_in_proc_t* p, int len, op_in_proc_t in);