/*
What does this one need to get in?
an int array
lowest value it can put
how many values left to put.
a function to print the array at the end. 
*/

int each_tuple(
    int* array,
    int index,
    int max_index,
    int lowest_value,
    int max_value,
    void (*f)(int*,int)
);