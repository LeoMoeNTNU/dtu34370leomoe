#include "types.h"

void inner_swap(int_arr_t original_array, int_arr_t indexes_to_swap, int orig_val,info_t info);

        original array. 
        array of indexes. 
        The order to swap it in. 
        original value. 
        info. 
        based on this, it will find the new one. 

        how I will do this. 
        I will just send it the solution and then it will be fine. 
